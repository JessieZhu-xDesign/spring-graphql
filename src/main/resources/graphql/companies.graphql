type Query {
   companyById(id:ID): Company
}

type Company {
    name: String!
    number_of_employees: Int
}

type Query {
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}